# Настройка Google Gemini API

## ✅ Что было сделано:

1. **Заменен OpenAI на Google Gemini**
   - Обновлен `requirements.txt` - добавлена библиотека `google-generativeai`
   - Обновлен `main.py` - заменен клиент OpenAI на Gemini
   - Обновлена обработка ошибок для Gemini API

2. **Настроены модели Gemini**
   - Используются модели: `models/gemini-2.5-flash`, `models/gemini-2.0-flash`, `models/gemini-pro-latest`
   - Система автоматически пробует разные модели, если одна не работает

3. **Настроен формат ответа**
   - Gemini настроен на возврат JSON ответов
   - Добавлена обработка markdown блоков кода в ответах

## 📝 Настройка .env файла

Убедитесь, что в файле `.env` есть:

```
GEMINI_API_KEY=ваш_ключ_gemini
```

Или можно использовать:

```
GOOGLE_API_KEY=ваш_ключ_gemini
```

## 🔧 Проверка работы

Запустите тест:
```bash
python test_gemini.py
```

Если видите "OK: API ключ работает!" - все настроено правильно.

## 🚀 Запуск сервера

```bash
python main.py
```

Или используйте:
```bash
run_server.bat
```

## 📚 Получение API ключа Gemini

1. Перейдите на: https://makersuite.google.com/app/apikey
2. Войдите в Google аккаунт
3. Создайте новый API ключ
4. Скопируйте ключ в файл `.env` как `GEMINI_API_KEY=ваш_ключ`

## ⚠️ Отличия от OpenAI

- Gemini использует другой формат запросов
- Модели имеют префикс `models/`
- JSON формат может быть обернут в markdown блоки кода
- Ошибки могут отличаться (обработаны в коде)



